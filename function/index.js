const {nextMap} = require('./next')
const {nextCellStatus} = require('./status')
const {round} = require('./round')
const {initMap} = require('./init')

module.exports = {
    nextMap,
    nextCellStatus,
    round,
    initMap
}